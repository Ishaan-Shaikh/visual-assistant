# Extra dependencies for the local LLaVA backend (--backend local).
# Requires a CUDA GPU with ~6 GB VRAM.
#
# Install with:
#   pip install -r requirements-local.txt

torch>=2.0.0
transformers>=4.37.0
accelerate>=0.27.0
bitsandbytes>=0.42.0